apiVersion: v1
kind: Service
metadata:
  name: {{ include "gutenberg-narrator.fullname" . }}
  labels:
    {{- include "gutenberg-narrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gutenberg-narrator.selectorLabels" . | nindent 4 }}
